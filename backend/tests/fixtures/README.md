# Generic reconstruction fixtures

`generate_fixtures.py` creates 12 small programmatic fixtures without reusing
the党建 regression image: simple text, business, technology, academic,
infographic, flowchart, cards, table, chart, poster, dark theme, and light
theme. The fixtures are intentionally simple and deterministic so CI can run
Scene Graph, geometry, and PPTX structural checks without downloading assets.
