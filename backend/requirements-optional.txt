# Optional high-accuracy OCR. Installation is best effort on Windows/Python versions
# where PaddlePaddle has a compatible wheel.
paddleocr>=2.8
paddlepaddle>=2.6
# Optional neural inpainting. The application falls back to OpenCV when unavailable.
simple-lama-inpainting>=0.1

